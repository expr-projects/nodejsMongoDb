doctype html
html(lang="en")
  head
    title All saved images
  body
    a(href="/") Home Page
    h1 List of Images Saved in MongoDB:
    if images.length <= 0
      p No images saved in MongoDB.
    else
      ul
        each image in images
          li
            a(href="#{image._id}")= image.title
            img(src="data:#{image.image.contentType};base64,#{image.image.data}", width="450", height="450", title="This image was reduced in the HTML to 450px in height and 450px in width.", alt="Image from server")
